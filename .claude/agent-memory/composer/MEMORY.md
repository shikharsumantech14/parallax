# Composer agent memory

One line per memory; detail lives in the topic files, read on demand. Scope:
only what the repo cannot say about itself. The schema, the catalog, the voice
contract and the floors are all in the repo — do not copy them here.

- [Kind fit by argument shape](kind-fit-by-argument-shape.md) — which kinds actually carried which argument, and the swaps that kept working
- [Dossier data gaps](dossier-data-gaps.md) — the kinds that get wanted and blocked, and the `DATA` field that blocks each
- [Rewrite runs](rewrite-runs.md) — how a Phase 4 rewrite storyboard differs from a fresh one; the log of storyboards sent back
