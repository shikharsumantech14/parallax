# Verifier memory — Parallax

One line per memory. Patterns only — nothing derivable from the repo (the
schema, the catalog, the source allowlists, the agent definition, or any
specific issue's facts lives better in its own file and would rot here).

- [Inherited-data blindspot](inherited-data-blindspot.md) — carried-over `data.values` arrays are the claims nobody has ever traced
- [Cheap checks that catch most](cheap-checks-that-catch-most.md) — the four highest-yield checks, and the two that reliably find nothing
- [Flag severity calibration](flag-severity-calibration.md) — how to report a ❌ that is paperwork, and when the lexicon is wrong instead of the draft
